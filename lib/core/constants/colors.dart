import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF000000); // Pure Black
  static const Color surface = Color(0xFF121212);    // Dark Grey for Cards
  static const Color primary = Colors.purple;        // Main theme color
  static const Color accent = Color(0xFFFFD700);     // Gold color for Crown/Premium
  static const Color textMain = Colors.white;
  static const Color textSecondary = Colors.grey;
}